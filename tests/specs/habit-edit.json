[
  { "name": "edit needs a selected habit", "steps": [{ "send": { "callback": "habit:edit" }, "expect": [{ "method": "sendMessage", "payload": { "text": "Choose a habit from My habits first." } }] }] }
]
