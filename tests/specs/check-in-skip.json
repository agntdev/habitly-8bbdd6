[
  { "name": "skip without a valid reminder is safe", "steps": [{ "send": { "callback": "check_in:skip" }, "expect": [{ "method": "sendMessage", "payload": { "text": "That reminder is no longer available. Open My habits to check in." } }] }] }
]
