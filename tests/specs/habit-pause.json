[
  { "name": "pause needs a selected habit", "steps": [{ "send": { "callback": "habit:pause" }, "expect": [{ "method": "sendMessage", "payload": { "text": "Choose a habit from My habits first." } }] }] }
]
