[
  { "name": "postpone without a valid reminder is safe", "steps": [{ "send": { "callback": "check_in:remind_later" }, "expect": [{ "method": "sendMessage", "payload": { "text": "That reminder is no longer available. Open My habits to check in." } }] }] }
]
