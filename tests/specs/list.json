[
  { "name": "habits list has an encouraging empty state", "steps": [{ "send": { "callback": "habits:list" }, "expect": [{ "method": "editMessageText", "payload": { "text": "No habits yet — tap New habit to create one." } }] }] }
]
