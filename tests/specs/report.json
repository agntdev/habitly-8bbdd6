[
  { "name": "weekly recap handles no habits", "steps": [{ "send": { "callback": "habits:report" }, "expect": [{ "method": "editMessageText", "payload": { "text": "No weekly recap yet — create a habit and your progress will appear here." } }] }] }
]
